/**
 * Central nav config for scalability: add/remove items or change paths in one place.
 */
export const NAV_LINKS = [
  { label: "Login", path: "/login" },
  { label: "Sign Up", path: "/signup" },
] as const;

export const LOGO_LINK_PATH = "/";
